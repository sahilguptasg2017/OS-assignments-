#include <stdio.h>
#include <stdint.h>
#include <math.h>
int main(){
    long long int count = 1 ;
    long long int max = pow(2, 32) ;
    while(count < max){
        count++;
    }
    //printf("%lldd",count);
    return 0;
}
